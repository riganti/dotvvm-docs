# Route Links with Icons

The simplest form of a `RouteLink` requires only a `RouteName` and `Text` property. It will navigate to the specified route when clicked. The `Type` property controls the visual appearance.

