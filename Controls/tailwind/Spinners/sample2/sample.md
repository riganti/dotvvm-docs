# Conditional Spinner

Use the `Visible` property to conditionally show or hide the spinner based on application state. This example demonstrates toggling a loading spinner with a button while also showing a loading message.

